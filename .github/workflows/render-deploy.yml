name: Render Deploy (Webhook)
on:
  push:
    branches: [ "main" ]
  workflow_dispatch: { }
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"